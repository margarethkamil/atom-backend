# Server configuration
PORT=3000

# CORS configuration
CORS_ORIGIN=http://localhost:4200

# Firebase configuration
# For local development, you need to set up the service account credentials
# Either set GOOGLE_APPLICATION_CREDENTIALS to the path to your service account key file
# Or use the Firebase emulator
GOOGLE_APPLICATION_CREDENTIALS=./path-to-service-account-key.json

# Optional: Firestore database URL
# FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com 